name: Build and Test

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - uses: actions/checkout@v1
      with:
        lfs: true
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - name: npm install, build, and test
      id: npm
      run: |
        npm ci
        npm run build --if-present
        npm test
      continue-on-error: true
      env:
        CI: true
    - name: Archive report from Backstop.js
      uses: actions/upload-artifact@v2
      with:
        name: reports
        path: report/backstopjs/ci_report/xunit.xml
    - name: Archive report from ESLint
      uses: actions/upload-artifact@v2
      with:
        name: reports
        path: report/eslint.xml
    - name: Check Result
      if: steps.npm.outcome == 'failure'
      run: exit 1
